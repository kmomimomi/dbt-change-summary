version: 2

seeds:
  - name: seed_sample_1
    description: "This is a seed file for seed_sample_1.csv."
    config:
      quote_columns: false
      column_types:
        id: integer
        name: string
